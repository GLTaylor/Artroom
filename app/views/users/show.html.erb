<h1>My room</h1>
<%= @user.user_name %>

<div class="ball-container">
  <% @user.hangings.each do |hanging| %>
    <%= cl_image_tag hanging.artwork.image, class: "image-small image-rounded" %>
  <% end %>
</div>
